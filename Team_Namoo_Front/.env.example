# 이 파일을 .env 로 복사해서 값을 채운다 (.env 는 git 에 안 올라감).
#   cp .env.example .env
#
# docker compose 와 vite 둘 다 이 디렉터리의 .env 를 자동으로 읽는다.

# 백엔드 API 주소.
#  - 로컬 전체 스택(루트 docker compose)         : http://localhost:8080
#  - 백엔드를 호스트에서 직접 실행               : http://localhost:8080
#  - 배포된 백엔드에 붙여서 프론트만 볼 때        : https://15-165-118-162.sslip.io
VITE_API_BASE_URL=http://localhost:8080
